@model Nortwind.Entities.Cart

<div>

    <span>
        <b>Your Cart(@Model.Lines().Sum(x=>x.CartUrunSayisi)</b>
        @Model.TotalPrice().ToString("c");

    </span>
    @Html.ActionLink("Checkout", "Index", "Cart");
</div>
